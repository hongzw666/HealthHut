<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.china.HealthHut.appmapper.AppRegisterMapper">

	<!-- 添加挂号记录 -->
	<insert id="addRegister" parameterType="Register">
		INSERT INTO
		register (disease_time, st_number, name, age, sex, telephone, isstudent, first_further)
		VALUES (#{disease_time},#{st_number},#{name},#{age},#{sex},#{telephone},#{isstudent},#{first_further})
	</insert>

</mapper>